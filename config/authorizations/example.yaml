# EXAMPLE authorization record (unsigned — illustrative only).
#
# A usable record MUST be signed. Generate a real one with:
#
#   export ANVIL_AUTH_SIGNING_KEY=<long-random-value>
#   anvil init-auth \
#     --engagement ACME-2026-Q3 \
#     --by sean@noisebeard.com \
#     --reason "Q3 compliance scan (JIRA SEC-1234)" \
#     --days 14 \
#     --repo /path/to/checkout \
#     --domain staging.acme.example \
#     --include-subdomains \
#     --ip-range 10.10.0.0/16 \
#     --out config/authorizations/acme-2026-q3.yaml
#
# The scope below is the *entire* allowlist — anything not listed is refused.

engagement_id: ACME-2026-Q3
authorized_by: sean@noisebeard.com
reason: "Q3 compliance scan (JIRA SEC-1234)"
scope:
  repos:
    - /path/to/checkout
  domains:
    - staging.acme.example
  include_subdomains: true
  ip_ranges:
    - 10.10.0.0/16
created_at: 2026-09-01T00:00:00+00:00
expires_at: 2026-09-15T00:00:00+00:00
signature: null   # populated by `anvil init-auth`; ScopeGuard rejects unsigned records
